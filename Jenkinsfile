pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building SafeHer'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying SafeHer'
            }
        }
    }
}
