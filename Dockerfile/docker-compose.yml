version: '3'

services:

  frontend:
    image: safeher-frontend
    ports:
      - "80:80"

  backend:
    image: safeher-backend
    ports:
      - "5000:5000"

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root